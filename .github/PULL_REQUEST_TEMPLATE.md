## Summary

<!-- What does this PR do, and why? Link the issue it closes, e.g. "Closes #123". -->

## Changes

<!-- Bullet list of the notable changes. -->

-

## How tested

<!-- Commands run, test names added/updated, or manual verification steps. -->

## Checklist

- [ ] `make build`, `make lint`, and `make test` pass (plus the PWA builds/tests if `packages/` or `apps/` changed — see CONTRIBUTING.md)
- [ ] `CHANGELOG.md` updated if the change is user-facing
- [ ] `packages/pwa-views/package.json` version bumped (+ its CHANGELOG entry) if that package changed — CI enforces this
- [ ] Docs updated (`AGENTS.md`, `docs/`) if architecture, conventions, or verification commands changed
