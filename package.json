{
  "name": "kyber-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "kyber repo workspaces root — see packages/ and apps/",
  "engines": {
    "node": ">=26 <27"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "overrides": {
    "esbuild": "^0.28.1"
  }
}
