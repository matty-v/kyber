# Claude Code repository instructions

The canonical repository instructions are in [`AGENTS.md`](AGENTS.md). Read and
follow that file before making changes.

`CLAUDE.md` is intentionally only this compatibility pointer so Claude Code and
other agents use one maintained source of truth. Update `AGENTS.md`, not this
file, when repository architecture, conventions, verification commands, or
gotchas change.
